name: Auto Publish Articles

on:
  schedule:
    # Exécute tous les jours à 18h00 UTC (19h00 heure française en hiver, 20h00 en été)
    - cron: '0 18 * * *'
  workflow_dispatch: # Permet de déclencher manuellement

permissions:
  contents: write # Nécessaire pour push les changements

jobs:
  publish:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0 # Nécessaire pour git diff
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci
      
      - name: Run auto-publish script
        working-directory: ./frontend
        run: node scripts/auto-publish-articles.js
        continue-on-error: false
      
      - name: Check for changes
        id: git-check
        run: |
          git diff --exit-code frontend/content/blog/ frontend/content/blog/draft/ || echo "has_changes=true" >> $GITHUB_OUTPUT
      
      - name: Commit and push changes
        if: steps.git-check.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add frontend/content/blog/ frontend/content/blog/draft/
          git status
          git commit -m "🤖 Auto-publish articles scheduled for $(date +%Y-%m-%d)" || exit 0
          git push origin HEAD:main || git push
      
      - name: No changes
        if: steps.git-check.outputs.has_changes != 'true'
        run: echo "✅ No articles to publish today"

